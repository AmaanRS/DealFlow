# The real API is the default. Set true only for isolated frontend development.
VITE_USE_MOCK_AUTH=false

# Used only by the Vite development proxy. Docker routes through APISIX directly.
VITE_API_PROXY_TARGET=http://localhost:9080
